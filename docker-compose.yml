services:
  exporter:
    build: ./exporter
    ports:
      - "8080:8080"

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    environment:
      EXPORTER_BASE_URL: http://exporter:8080
    depends_on:
      - exporter
