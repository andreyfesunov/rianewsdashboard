services:
  exporter:
    build: ./exporter
    ports:
      - "8000:8000"

  dashboard:
    build: ./dashboard
    ports:
      - "3000:3000"
    environment:
      EXPORTER_BASE_URL: http://exporter:8000
    depends_on:
      - exporter
